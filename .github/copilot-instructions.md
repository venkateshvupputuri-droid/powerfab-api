# PowerFab API

- Use TypeScript, Express, Prisma, and SQLite for this project.
- Keep fabrication status changes auditable through `StatusHistory`.
- QR identifiers must remain unique and should not expose internal database details.
- Run `npm run build` after implementation changes.
